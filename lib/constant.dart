import 'package:flutter/material.dart';

const appTitle = 'App';
const appBarHeight = kToolbarHeight + 12.0;
const accountIconSize = 40.0;
const bottomNavigationBarHeight = 80.0;
const double borderRadius = 10.0;
const sonyBlack = Color(0xFF1F191D);
const sonyRed = Color(0xFFBC1A0A);
const itemBackgroundColor = Color.fromRGBO(240, 240, 240, 1);

GlobalKey<NavigatorState> pageNavigatorKey = GlobalKey<NavigatorState>();